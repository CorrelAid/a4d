---
id: 18
title: Triage every flagged R/Python difference for both arms, and resolve the 189-vs-155-tracker discrepancy
labels: [wayfinder:task]
status: open
blocked_by: []
assignee: null
claimed_at: null
resolution: null
evidence: null
closed_by: null
spawned_by: 17
---

## Premise

Rests on [Fix the product comparison's row-alignment key, then triage every
flagged R/Python difference](17-fix-product-row-alignment-and-triage.md),
closed: the product arm's row-alignment key is fixed (ordinal position
within `(clinic_id, product_sheet_name)`, computed at comparison time in
`add_row_ordinal()`, `src/a4d/migration/compare.py`) and validated directly
against the real R/Python output pair — 97.2% row-key match on cleaned
product (up from near-0%), with the `product_sheet_name` mismatch count
(201) now reproducing the parity-presentation PDF's number exactly. The
patient arm's key (`patient_id`, `sheet_name`) was already confirmed sound
in [ticket 15](15-build-and-run-comparison-script.md).

That session ran a full comparison with the fixed key and found the
per-column mismatch counts are still far larger than the parity-presentation
PDF's numbers even with alignment fixed: `product_category` 13,638 vs the
PDF's 214, `product_entry_date` 10,424 vs 559, `product_balance` 2,343 vs
480 (all from the `product_data_cleaned` stage's `per_column` sheet,
generated by `just compare-outputs` against
`/Volumes/USB SanDisk 3.2Gen1 Media/a4d/output_r` and `output_python`). A
first look at `product_category` found the mismatches are systematically
`r_value=None` where Python has a real category value — not yet understood
whether that's a genuine R behavior (never populates `product_category` for
some file/year range) or a remaining artifact. This ticket inherits that as
an open question, not a diagnosis.

Also inherited, unreconciled since ticket 15: the parity-presentation PDF
cites 189 trackers / 61,077 rows for product; the currently-frozen
`output_r/` on the drive has 155 product-cleaned files totalling 47,644
rows — confirmed unchanged by ticket 17's alignment fix, so the gap isn't
an artifact of the old broken key. Whether this is a stale PDF number or a
genuinely different drive snapshot at the time the PDF was built is not yet
investigated.

## Question

Go through the flagged differences from `just compare-outputs`
(`r_dir py_dir output_dir --only-mismatches`, ticket 19: writes one
timestamped run folder under `output_dir`, four Excel reports plus history
snapshots inside it — re-run to get fresh reports, since the tool changed
after the reports already on the USB drive were generated) for both arms,
column by column, and determine why R and Python disagree —
labeling causes into the classifier registry
(`src/a4d/migration/compare.py`'s `classify`/`*_CLASSIFIERS`) as they're
identified, falling back to the original source Excel trackers
(`a4dphase2_upload` on the test-data drive) as the arbiter wherever a
divergence isn't already an understood, expected pattern (per the map's
standing preference: the migration is not 1:1 R-parity, R can be wrong).

Start with `product_category` (currently the single largest count, and
already shows a suspicious pattern — R null where Python has a value) and
`product_entry_date` (the PDF's largest known category, now finally visible
after ticket 17's fix) before the smaller columns. Carries forward ticket
15/17's validation requirement: the script's per-column and per-cause counts
should reproduce (or the session should be able to explain any deltas from)
the parity-presentation PDF's numbers — `Product_entry_date` (559:
typo-rescue 408, CE-typo 71, sentinel-null 66, off-by-one-day 7),
`Product_balance` (480), `Product_category` (214), `Product_sheet_name`
(201, already reproduced exactly), `Product_received_from` (154),
`Product_units_received` (9). Resolving the 189-vs-155-tracker discrepancy
is in scope here too, since it bears directly on whether the PDF's
per-column counts are even comparable to this tool's — it may turn out the
PDF's baseline included trackers this session's `output_r/` doesn't have,
in which case some deltas above are expected, not defects.

Patient-arm triage (raw and cleaned, both stages, alignment already sound)
is in scope here too, since it was run in ticket 15 but never triaged.

This ticket is large and investigative (up to ~47,644 product rows' worth
of triage across multiple columns, plus the full patient arm) — if it
doesn't converge in one session, split the remaining per-column
investigation into further tickets rather than leaving it open-ended.
